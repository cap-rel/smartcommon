const smart = {
    labelProps: {
        className: `font-semibold`
    }
};

const reverseRow = {
    containerProps: {
        className: `flex-row-reverse justify-end gap-4`
    },
}

export const booleanVariants = { smart, reverseRow };