import { floor, isDate } from "lodash";

// export function toSTimestamp(date) {
//     return floor((isDate(date) ? date.getTime() : date) / 1000);
// }

// export function toMsTimestamp(date) {
//     return isDate(date) ? date.getTime() : (date * 1000);
// }