import { setTestStory } from "../../../../../../storybook";
import { propTypes } from "../../props";

export const Signature = setTestStory({
    args: {
    },
    props: propTypes,
    hidden: ["id"]
});
