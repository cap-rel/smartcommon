import { setTestStory } from "../../../../../../storybook";
import { propTypes } from "../../props";

export const Duration = setTestStory({
    args: {
        value: 3661,
    },
    props: propTypes,
    hidden: []
});
