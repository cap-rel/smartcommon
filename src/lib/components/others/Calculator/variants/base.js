export const compact = () => ({
    showHistory: false,
    showMemory: false,
});

export const minimal = () => ({
    showHistory: false,
    showMemory: false,
    showFab: false,
    showOverlay: false,
});

export const centered = () => ({
    position: "center",
});

export const noFab = () => ({
    showFab: false,
});
